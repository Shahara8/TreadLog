<UserControl x:Class="TreadLog.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppBackground}">

    <StackPanel Margin="32,28,32,28" MaxWidth="480" HorizontalAlignment="Left">

        <!-- ── Header ──────────────────────────────────────────────────────── -->
        <TextBlock Text="Settings" FontSize="26" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Text="Preferences and data options"
                   FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,32"/>

        <!-- ── Distance unit ────────────────────────────────────────────────── -->
        <Border Background="{StaticResource CardBackground}" CornerRadius="10"
                Padding="24,20" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Distance Unit" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock Text="Choose the unit used to display distances throughout the app."
                           FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal">
                    <RadioButton Content="Kilometres (km)" GroupName="DistUnit"
                                 IsChecked="{Binding DistanceUnit,
                                     Converter={StaticResource StringEqualConverter},
                                     ConverterParameter=km}"
                                 Margin="0,0,24,0"/>
                    <RadioButton Content="Miles (mi)" GroupName="DistUnit"
                                 IsChecked="{Binding DistanceUnit,
                                     Converter={StaticResource StringEqualConverter},
                                     ConverterParameter=mi}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ── Save / status ────────────────────────────────────────────────── -->
        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Save Settings"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding SaveCommand}"/>

            <TextBlock VerticalAlignment="Center" Margin="16,0,0,0"
                       FontSize="13"
                       Text="{Binding StatusMessage}"
                       Foreground="{StaticResource SuccessBrush}"
                       Visibility="{Binding SaveSucceeded,
                           Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

    </StackPanel>
</UserControl>
